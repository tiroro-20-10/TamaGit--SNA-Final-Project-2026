# GitHub repository to watch (owner/repo-name)
GITHUB_REPO=owner/repository

# Webhook secret — generate with: openssl rand -hex 32
GITHUB_WEBHOOK_SECRET=123

# Optional: override where pet state is stored (default: ~/.tamagit/state.json)
TAMAGIT_STATE_PATH=~/.tamagit/state.json
