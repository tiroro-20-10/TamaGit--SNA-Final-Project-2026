services:
  gittama:
    build:
      context: .
      target: runtime
    working_dir: /workspace
    environment:
      PYTHONPATH: /workspace:/app
    volumes:
      - ~/.gittama:/root/.gittama
      - .:/workspace
    command: status
    # docker compose run --rm gittama scan
